<!doctype html>
<html>

<head>
    <% include includes/head.ejs %>
</head>

<body>
    <header>
        <% include includes/header.ejs %>
    </header>


    <div class="submit-form-container">
        <div class="col-12">





            <div class="notification">

                    <%=visit.activitylist %>

                <form action="" method="POST">
                    <br>
                    <br> Please select the activity you participated in today:
                    <br>
                    <% for(var i=0; i < activities.length; i++) { %>
                        <label>
                            <input type="radio" name="activitylist" value="<%= activities[i].activity %>" <%= visit.activitylist==activities[i].activity ? "checked" : "" %>/><%= activities[i].activity %> 
                        </label>
                        <% } %>
                    
                    <br>
                    <br>
                    <h3>Pre-session:</h3>
                    <p>
                        <i>This section refers to how you were feeling before
                            <span class="activity">activity</span>.</i>
                    </p>
                    <br>
                    <br> Today, I felt:
                    <br>
                    <label>
                        <input type="radio" name="presession" value="great" <%= visit.presession=="great" ? "checked" : "" %>/>Great
                    </label>
                    <label>
                        <input type="radio" name="presession" value="good" <%= visit.presession=="good" ? "checked" : "" %>/>Good
                    </label>
                    <label>
                        <input type="radio" name="presession" value="ok" <%= visit.presession=="ok" ? "checked" : "" %>/>OK
                    </label>
                    <label>
                        <input type="radio" name="presession" value="ill" <%= visit.presession=="ill" ? "checked" : "" %>/>Ill
                    </label>
                    <br>
                    <br> Overall, I feel that my health is:
                    <br>
                    <label>
                        <input type="radio" name="health" value="very good" <%= visit.health=="very good" ? "checked" : "" %>/>Very good
                    </label>
                    <label>
                        <input type="radio" name="health" value="good" <%= visit.health=="good" ? "checked" : "" %>/>Good
                    </label>
                    <label>
                        <input type="radio" name="health" value="bad" <%= visit.health=="bad" ? "checked" : "" %>/>Bad
                    </label>
                    <label>
                        <input type="radio" name="health" value="very bad" <%= visit.health=="very bad" ? "checked" : "" %>/>Very bad
                    </label>
                    <br>
                    <br>
                    <h3>Post-session:</h3>
                    <p>
                        <i>This section refers to how you felt after
                            <span class="activity">activity</span>.</i>
                    </p>
                    <br> The
                    <b>BEST</b> part of
                    <span class="activity">activity</span> was:
                    <br>
                    <input type="text" value="<%= visit.bestpart %>" name="bestpart">
                    <br>
                    <br> The
                    <b>WORST</b> part of
                    <span class="activity">activity</span> was:
                    <br>
                    <input type="text" value="<%= visit.worstpart %>" name="worstpart">
                    <br>
                    <br> After participating in
                    <span class="activity">activity</span>, I would do
                    <span class="activity">activity</span> on my own.
                    <br>
                    <label>
                        <input type="radio" name="postsession" value="strongly agree" <%= visit.postsession=="strongly agree" ? "checked" : "" %>/>Strongly Agree
                    </label>
                    <label>
                        <input type="radio" name="postsession" value="agree" <%= visit.postsession=="agree" ? "checked" : "" %>/>Agree
                    </label>
                    <label>
                        <input type="radio" name="postsession" value="disagree" <%= visit.postsession=="disagree" ? "checked" : "" %>/>Disagree
                    </label>
                    <label>
                        <input type="radio" name="postsession" value="strongly disagree" <%= visit.postsession=="strongly disagree" ? "checked" :
                            "" %>/>Strongly Disagree
                    </label>
                    <br>

                    <br>
                    <input type="submit" class="approvevisit" value="Submit" aria-label="This submits the approved visit.">

                    <a href="../profile">
                        <button class="cancel-update" aria-label="This cancels the visit updates.">Cancel</button>
                    </a>


                    <br>
                    <h3>Thank you for completing the Passport to Lifetime Fitness!</h3>
                    <br>
                </form>


            </div>
            <% if (visit) { %>
                <% if (visit.approved) { %>
                    Approved!
                    <% } else {    %>
                        Unapproved
                        <% } %>
                            <% } %>

        </div>
    </div>
    </div>
    <footer>
        <% include includes/footer.ejs %>
    </footer>
</body>

</html>